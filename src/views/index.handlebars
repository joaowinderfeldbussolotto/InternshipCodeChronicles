    {{#each gifs}}
    <img src="{{ this.url }}" alt="{{ this.title }}" style="display: inline-block; margin-right: 10px;">
    {{/each}}

    <h1>{{ wcomic.title }}</h1>

    <img src="{{ wcomic.url }}" alt="" />
    <p > {{ wcomic.description }} </p>

